#ifndef WIFI_CREDENTIALS_H
#define WIFI_CREDENTIALS_H

// Copy this file to wifi_credentials.h and fill in your WiFi.
// wifi_credentials.h is gitignored so you don't commit secrets.

#define WIFI_SSID     "your-ssid"
#define WIFI_PASSWORD "your-password"

#endif
